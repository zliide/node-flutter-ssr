{
  "name": "flutter-server-side-renderer",
  "type": "module",
  "version": "0.0.1",
  "description": "A server-side renderer for Flutter for SEO optimization",
  "files": [
    "*.js",
    "*.d.ts",
    "*.js.map"
  ],
  "exports": {
    ".": "./index.js"
  },
  "devDependencies": {
    "@types/node": "^14",
    "@types/mocha": "^9.0.0",
    "mocha": "^9.1.3",
    "nodemon": "^2.0.15",
    "tslint": "^6.1.3",
    "typescript": "4.5.4"
  },
  "scripts": {
    "prepare": "tsc",
    "build": "tsc && tslint -p tsconfig.json && mocha",
    "test": "mocha",
    "mon": "nodemon -e ts --ignore \"**/*.d.ts\" --exec \"npm run build || exit 1\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zliide/node-flutter-ssr.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zliide/node-flutter-ssr/issues"
  },
  "homepage": "https://github.com/zliide/node-flutter-ssr#readme"
}